#include <bits/stdc++.h>
using namespace std;

int main() {
    int n,m,k; cin >> n >> m >> k;

    vector<int> a(m);
    vector<int> b(m);

    for(int i=0;i<m;i++){
        cin >> a[i] >> b[i];
    }
    for(int i=0;i<k;i++){
        cin >> c[i] >> d[i];
    }

    

}
